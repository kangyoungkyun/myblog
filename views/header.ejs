  <!-- Navigation -->
  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
              </button>
              <a class="navbar-brand" href="/">위캔두코딩</a>
          </div>
          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
              <ul class="nav navbar-nav">
                    <%if (whoami != undefined) { %>
                        <%if (whoami == 'abcnt') { %>
                            <li>
                                <a href="/write/bigwrite">대분류(강좌개설)</a>
                            </li>
                            <li>
                                <a href="/write/middlewrite">중분류(소제목)</a>
                            </li>
                            <li>
                                <a href="/write/write">소분류(영상글쓰기)</a>
                            </li>
                            <li>
                                <a href="/write/writepost">포스터쓰기</a>
                            </li>
                            <li>
                                    <a href="/login/logout">로그아웃</a>
                            </li>
                        <% } %>   
                    <% } else { %>  

                        <!-- <li>
                                <a href="/login/login">로그인</a>
                        </li> -->

                    <% } %>   
                  <!-- <li>
                        <a href="/apply/apply">신청하기</a>
                 </li>
                 <li>
                        <a href="/apply/applypay">결제</a>
                 </li>
 
                <li>
                    <a href="/read/bigmodify">대분류조회</a>
                </li> -->
              </ul>

          </div>
          <!-- /.navbar-collapse -->
      </div>
      <!-- /.container -->
  </nav>

  <!-- Page Content -->
  <div class="container">


      <div class="row">

          <div class="col-md-3">

                <p class="lead">Colum</p>
                <div class="list-group">
                    <%if (rows != undefined) { %>
                        <% for (var i = 0; i < rows.length; i++) {  %>
                            <%if (rows[i]['close'] != 'true' && rows[i]['section'] == '글') { %>
                                <a href="/read/readbigmiddlegul?num=<%=rows[i]['bignum']%>" class="list-group-item"><%=rows[i]['title']%><span class="badge"><%=rows[i]['cnt']%></span></a>
                            <% } %>
                        <% } %>
                    <% } else { %>
                        <a href="#" class="list-group-item">ready...</a>
                    <% } %>
                </div>


              <!-- <p class="lead">Video</p>
              <div class="list-group">

            <%if (rows != undefined) { %>
                <% for (var i = 0; i < rows.length; i++) {  %>
                    <%if (rows[i]['close'] != 'true' && rows[i]['section'] == '영상') { %>
                        <a href="/read/readbigmiddle?num=<%=rows[i]['bignum']%>" class="list-group-item"><%=rows[i]['title']%><span class="badge"><%=rows[i]['cnt']%></span></a>
                    <% } %>
                <% } %>
            <% } else { %>
                <a href="#" class="list-group-item">ready...</a>
            <% } %>
              </div> -->
          </div>